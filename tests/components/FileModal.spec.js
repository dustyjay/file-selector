// const { render } = require('@testing-library/vue');
// import FileModal from '../../src/components/FileModal.vue';

// test('There is button to select files', async () => {
//   const { getByText } = render(FileModal);
//   const button = getByText('Select files');
//   expect(button.tagName).toBe('BUTTON');
//   expect(button).toBeTruthy();
// });
